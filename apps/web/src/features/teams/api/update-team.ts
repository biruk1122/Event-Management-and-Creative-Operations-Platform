import type { UpdateTeam } from "../lib/teams-outcome";

/** Placeholder for `PATCH /api/v1/teams/:id`. TEAM-05 wires the real call. */
export const updateTeam: UpdateTeam = () =>
  Promise.resolve({ status: "unexpected" });
