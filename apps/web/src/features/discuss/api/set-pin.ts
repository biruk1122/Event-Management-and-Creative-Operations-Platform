import type { SetPin } from "../lib/discuss-outcome";

/** Placeholder for `PUT/DELETE .../messages/:messageId/pin` - see DSC-05. */
export const setPin: SetPin = () => Promise.resolve({ status: "unexpected" });
